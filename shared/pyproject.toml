[project]
name = "disaster-shared"
version = "0.1.0"
description = "Shared utilities for disaster microservices"
requires-python = ">=3.13"

[tool.uv]
package = true

[tool.ruff.lint]
select = ["ALL"]
ignore = ["T201", "D203", "D212", "COM812"]

[tool.ty.src]
include = [
    "src",
]
