[tool.uv.workspace]
members = [
    "shared",
    "gateway",
    "earthquakes",
    "tornadoes",
    "cyclones",
    "fires",
]

[tool.uv.sources]
disaster-shared = { workspace = true }

[dependency-groups]
dev = [
    "requests>=2.32.5",
]
