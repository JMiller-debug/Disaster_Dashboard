[project]
name = "gateway"
version = "0.1.0"
description = "API gateway for disaster dashboard microservices"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "uvicorn>=0.42.0",
]

[tool.uv]
package = true